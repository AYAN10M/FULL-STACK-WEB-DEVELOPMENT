// variable in javascript
var user_name = prompt("What is your name?");

// showing the user name using alert in a browser
alert(user_name);

// to see the type of variable
//typeof()
typeof(user_name);      // string
typeof(10);             // number
typeof(10.5);           // number
typeof(true);           // boolean
typeof(null);           // object
typeof(undefined);      // undefined

